import React from 'react';
import ReactDOM from 'react-dom/client';
import SurveyFrontend from './App.jsx'; // assuming your file is still App.jsx

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SurveyFrontend />
  </React.StrictMode>
);